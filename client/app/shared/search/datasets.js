import {sources} from './sources';

var datasets = {
  organizations: {
    name: 'organizations',
    source: sources.organizations,
    templates: {
      suggestion: 'replaceme'
    },
  }
};

export default datasets;
